# PRD: CCTV 화면 촬영 기반 주차장 점유율 웹앱

## 1. 개요

이 프로젝트는 휴대폰 웹앱 카메라로 컴퓨터 화면에 재생 중인 CCTV 주차장 영상을 촬영하고, 일정 간격으로 프레임을 분석하여 주차면별 점유 상태와 전체 점유율을 보여주는 웹앱을 만든다.

초기 목표는 실제 CCTV 스트림 직접 연동이 아니라, CCTV 화면 또는 CCTV 영상 재생 화면을 고정된 휴대폰 카메라로 촬영하는 MVP를 구현하는 것이다.

## 2. 문제 정의

현장 CCTV에 직접 접근하기 어렵거나 네트워크 제약이 있는 경우에도, 화면을 카메라로 촬영해 주차 점유 상태를 확인할 수 있어야 한다.

기존 단순 YOLO bbox 방식은 비스듬한 CCTV 각도, 인접 주차면 겹침, 차량 일부 가림 상황에서 주차면 점유 판단이 불안정할 수 있다. 따라서 `02_yolov8n-seg.py`의 YOLO segmentation, 차량 footprint, 주차면 polygon 교차 판단 방식을 기준으로 개발한다.

## 3. 목표

- 휴대폰 웹앱에서 카메라를 켜고 CCTV 화면을 촬영한다.
- 5분 간격으로 프레임 1장을 캡처해 서버로 전송한다.
- 서버에서 YOLO segmentation 기반으로 차량 영역을 분석한다.
- 사전에 등록된 주차면 polygon과 차량 영역의 겹침 비율을 계산한다.
- 주차면별 `occupied`, `empty`, `unknown` 상태를 표시한다.
- 전체 주차 점유율을 계산해 웹앱에 보여준다.
- 캡처 이미지는 저장 공간 관리를 위해 최근 10장만 보관하고, 장기 기록은 분석 결과 로그 중심으로 남긴다.

## 4. 사용자

- 설치자/개발자: 초기 주차면 좌표를 설정하고 테스트를 수행한다.
- 관리자/운영자: 촬영 화면과 점유율 결과를 확인한다.
- 교수님/검토자: MVP 동작 방식과 정확도, 확장 가능성을 검토한다.

## 5. 핵심 사용 시나리오

1. 컴퓨터에서 CCTV 주차장 영상 또는 연습 영상을 재생한다.
2. 휴대폰을 거치대에 고정하고, 웹앱에 접속한다.
3. 웹앱에서 카메라 권한을 허용한다.
4. 설정 모드에서 기준 프레임을 캡처한다.
5. 사용자가 주차면마다 4개 점을 찍고 ID를 부여한다.
6. 설정을 저장한다.
7. 모니터링 모드를 시작한다.
8. 웹앱이 5분마다 프레임 1장을 서버로 전송한다.
9. 서버가 주차면별 점유 상태와 전체 점유율을 반환한다.
10. 웹앱 화면에서 결과를 확인한다.
11. 서버는 분석 결과 로그를 저장하고, 캡처 이미지는 최근 10장만 유지한다.

## 6. MVP 범위

### 포함

- PC에서 실행되는 Python 분석 서버
- 휴대폰 브라우저에서 접속하는 웹앱 UI
- 브라우저 카메라 연결
- 기준 프레임 캡처
- 수동 주차면 polygon 등록
- 주차면 설정 JSON 저장/불러오기
- 5분 간격 프레임 1장 캡처 및 서버 전송
- YOLO segmentation 기반 차량 검출
- 주차면별 점유 판정
- 전체 점유율 표시
- 분석 결과 로그 저장
- 캡처 이미지 최근 10장 보관 및 자동 정리

### 제외

- 실제 CCTV 스트림 직접 연동
- 자동 주차면 인식
- 모바일 기기 안에서 직접 YOLO 추론
- 사용자 계정/권한 관리
- 클라우드 배포
- 실시간 초당 프레임 분석

## 7. 기능 요구사항

### FR-001. 카메라 연결

웹앱은 브라우저 `getUserMedia` API를 사용해 휴대폰 카메라를 연결한다.

### FR-002. 기준 프레임 캡처

사용자는 현재 카메라 화면에서 기준 이미지를 캡처할 수 있어야 한다.

### FR-003. 주차면 등록

사용자는 기준 이미지 위에서 주차면마다 4개 점을 클릭해 polygon을 생성할 수 있어야 한다.

### FR-004. 주차면 ID 부여

각 주차면에는 `A1`, `A2`, `B1` 같은 ID를 지정할 수 있어야 한다.

### FR-005. 설정 저장

등록된 주차면 좌표와 분석 옵션은 JSON 파일로 저장되어야 한다.

### FR-006. 설정 불러오기

모니터링 시작 시 저장된 주차면 설정을 불러와야 한다.

### FR-007. 주기적 프레임 전송

웹앱은 기본 5분 간격으로 카메라 프레임 1장을 캡처해 서버로 전송해야 한다. 영상 전체를 저장하거나 전송하지 않는다.

### FR-008. 차량 검출

서버는 YOLO segmentation 모델로 차량 객체를 검출해야 한다. 기본 대상 클래스는 `car`, `truck`, `bus`, `motorcycle`이다.

### FR-009. 점유 판정

서버는 차량 footprint와 주차면 polygon의 교차 면적 비율을 계산해 점유 여부를 판단해야 한다.

### FR-010. 상태 표시

웹앱은 각 주차면 상태를 `occupied`, `empty`, `unknown`으로 표시해야 한다.

### FR-011. 점유율 표시

웹앱은 전체 점유율을 `occupied / total * 100` 기준으로 표시해야 한다.

### FR-012. 로그 저장

분석 결과는 시간, 주차면 ID, 상태, confidence, 점유율을 포함해 CSV 또는 JSON으로 저장해야 한다. 장기 보관은 이미지가 아니라 결과 로그를 기준으로 한다.

### FR-013. 캡처 이미지 보관 정책

서버는 분석에 사용한 캡처 이미지를 최근 10장까지만 보관해야 한다. 10장을 초과하면 가장 오래된 이미지를 자동 삭제한다. 단, 분석 결과 로그는 별도로 계속 저장할 수 있다.

## 8. 비기능 요구사항

- 분석 간격 기본값은 5분으로 한다.
- 영상 흔들림을 줄이기 위해 휴대폰은 고정 거치대 사용을 전제로 한다.
- 첫 MVP는 같은 Wi-Fi 환경의 PC 서버와 휴대폰 브라우저 조합을 전제로 한다.
- 휴대폰 브라우저 카메라 접근을 위해 HTTPS 또는 로컬 개발용 예외 구성이 필요할 수 있다.
- 분석 실패 또는 낮은 confidence 상황은 `unknown`으로 처리한다.

## 9. 분석 로직

1. 웹앱에서 프레임 이미지를 서버로 전송한다.
2. 서버가 YOLO segmentation을 실행한다.
3. 차량 mask에서 차량 바닥 footprint를 추정한다.
4. 필요 시 homography를 통해 BEV 좌표계로 변환한다.
5. 저장된 주차면 polygon과 차량 footprint의 교차 면적을 계산한다.
6. 교차 비율이 임계값 이상이면 `occupied`로 판단한다.
7. 교차 비율이 낮으면 `empty`로 판단한다.
8. confidence가 낮거나 경계 상황이면 `unknown`으로 판단한다.
9. 전체 점유율을 계산한다.
10. 분석 결과 로그를 저장한다.
11. 캡처 이미지는 최근 10장만 남기고 오래된 이미지는 삭제한다.

## 10. 데이터 구조 예시

### parking_config.json

```json
{
  "camera_id": "phone_camera_01",
  "analysis_interval_minutes": 5,
  "snapshot_retention_count": 10,
  "occupancy_threshold": 0.15,
  "slots": [
    {
      "id": "A1",
      "points": [[120, 340], [210, 330], [230, 430], [110, 440]]
    }
  ]
}
```

### analysis_result.json

```json
{
  "timestamp": "2026-07-27T23:00:00+09:00",
  "total_slots": 3,
  "occupied_slots": 2,
  "occupancy_rate": 66.7,
  "slots": [
    {
      "id": "A1",
      "status": "occupied",
      "score": 0.42
    }
  ]
}
```

## 11. 성공 기준

- 휴대폰 웹앱에서 카메라 화면이 정상 표시된다.
- 기준 프레임 위에 주차면을 수동 등록할 수 있다.
- 저장된 주차면 설정을 불러와 분석할 수 있다.
- 5분 간격 프레임 분석이 자동으로 수행된다.
- 주차면별 상태와 전체 점유율이 화면에 표시된다.
- 캡처 이미지는 최근 10장까지만 보관된다.
- 테스트 영상 기준으로 주차면 점유 판단 결과를 사람이 확인 가능한 수준으로 제공한다.

## 12. 리스크

- 컴퓨터 화면 촬영 시 반사광, 모아레, 해상도 저하가 발생할 수 있다.
- 휴대폰 위치가 변하면 저장된 주차면 좌표가 틀어질 수 있다.
- CCTV 영상 각도가 비스듬하면 인접 주차면 겹침 문제가 생길 수 있다.
- 차량 일부만 보이거나 검은색 차량처럼 검출이 어려운 케이스가 있을 수 있다.
- 브라우저 카메라 접근은 HTTPS 설정이 필요할 수 있다.

## 13. 향후 확장

- 실제 CCTV 스트림 연동
- 자동 주차면 후보 추천
- VLM 보조 판단
- 관리자 대시보드
- 장기 점유 로그 분석
- 현장별 설정 파일 관리
- 소형 PC 또는 엣지 디바이스 배포


