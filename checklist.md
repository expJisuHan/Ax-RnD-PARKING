# 주차장 점유율 웹앱 개발 체크리스트

체크 기준:

- `[x]`: 코드 구현 또는 자동 검증 완료
- `[ ]`: 테스트 영상, 휴대폰 실기기, 현장 환경 등 추가 작업 필요

## 현재 상태

- MVP 코드 구현: 완료
- 자동 테스트: 7개 통과
- 실제 YOLO 모델 API 스모크 테스트: 완료
- 휴대폰 실기기 및 현장 CCTV 정확도 검증: 미진행
- HTTPS 기반 휴대폰 접속: 미구성

## 0. 프로젝트 준비

- [x] `Ax-RnD-PARKING` 프로젝트 폴더 구조 정리
- [ ] 테스트 영상 `data/` 폴더에 정리
- [x] 교수님 예시 코드 `01_yolov8n.py`, `02_yolov8n-seg.py` 참고 위치와 개발 기준 문서화
- [x] Python 가상환경 `.venv` 생성
- [x] 필수 패키지 설치: `fastapi`, `uvicorn`, `ultralytics`, `opencv-python`, `numpy`, `python-multipart`
- [x] YOLO segmentation 모델 파일 준비: `models/yolov8n-seg.pt`

## 1. 백엔드 서버

- [x] FastAPI 서버 기본 구성
- [x] `/health` 상태 확인 API 작성
- [x] 프레임 이미지 업로드 및 분석 API 작성
- [x] 주차면 설정 저장 API 작성
- [x] 주차면 설정 불러오기 API 작성
- [x] 최근 분석 결과 반환 API 작성
- [x] CSV 및 JSONL 분석 로그 저장 기능 작성
- [x] 캡처 이미지 최근 10장 보관 기능 작성
- [x] 10장을 초과한 오래된 캡처 이미지 자동 삭제 기능 작성
- [x] 이미지 형식 및 15MB 업로드 크기 검증
- [x] 안전한 캡처 파일 조회 경로 검증

## 2. YOLO 분석 모듈

- [x] `02_yolov8n-seg.py` 핵심 로직을 이미지 분석 모듈로 분리
- [x] 모델 지연 로딩 및 재사용 함수 작성
- [x] 이미지 1장 입력 분석 함수 작성
- [x] 차량 클래스 필터링 적용: `car`, `truck`, `bus`, `motorcycle`
- [x] segmentation mask 추출 로직 작성
- [x] 차량 바닥 footprint 추정 로직 작성
- [x] 주차면 polygon 교차 면적 계산 로직 작성
- [x] 점유 및 판단 보류 임계값 적용
- [x] `occupied`, `empty`, `unknown` 상태 반환
- [x] 전체 점유율 계산
- [x] 모델 파일 누락 시 서버 중단 없이 `unknown` 반환
- [ ] Homography/BEV 좌표 보정 옵션 구현

## 3. 주차면 설정 기능

- [x] 기준 프레임 캡처 기능 구현
- [x] 캡처 이미지 위 canvas overlay 표시
- [x] 4점 클릭으로 주차면 polygon 생성
- [x] 주차면 ID 입력 UI 구현
- [x] 주차면 수정 기능 구현
- [x] 주차면 삭제 기능 구현
- [x] 마지막 선택 점 취소 기능 구현
- [x] 설정 JSON 저장 기능 구현
- [x] 설정 JSON 불러오기 기능 구현
- [x] 휴대폰 위치가 바뀌면 재설정이 필요하다는 안내 표시

## 4. 프론트엔드 웹앱

- [x] 브라우저 카메라 권한 요청
- [x] 후면 카메라 우선 선택
- [x] 실시간 카메라 preview 표시
- [x] 설정 모드 화면 구현
- [x] 모니터링 모드 화면 구현
- [x] 5분 간격 자동 캡처 타이머 구현
- [x] 모니터링 시작 시 즉시 1회 분석
- [x] 수동 분석 버튼 구현
- [x] 분석 중 및 오류 상태 표시
- [x] 최근 캡처 이미지 수 `현재 / 10장` 표시
- [x] 주차면별 상태 표시
- [x] 전체 점유율 표시
- [x] 마지막 분석 시간 표시
- [x] 데스크톱 및 모바일 반응형 UI 구현
- [x] 현재 설정과 다른 이전 분석 결과 숨김 처리

## 5. 분석 주기 및 안정화

- [x] 기본 분석 간격 5분 설정
- [x] 분석 간격을 JSON 설정값으로 옵션화
- [x] 최근 결과 히스토리를 CSV 및 JSONL로 저장
- [x] 캡처 이미지는 최근 10장만 유지하도록 정리 로직 적용
- [ ] 동일 상태가 2~3회 연속 확인될 때 상태를 확정하는 smoothing 적용
- [x] 임계값 경계 결과를 `unknown` 처리
- [x] YOLO confidence threshold 옵션화
- [x] occupancy 및 unknown threshold 옵션화
- [ ] 모니터링 중 브라우저 절전/백그라운드 타이머 대응

## 6. 실행 환경

- [x] PC 서버 실행 스크립트 `run.ps1` 작성
- [x] 프로젝트 전용 가상환경과 패키지 설치
- [x] 로컬 PC에서 서버 및 첫 화면 접속 확인
- [ ] 같은 Wi-Fi에서 휴대폰 접속 테스트
- [ ] 휴대폰 브라우저 카메라 HTTPS 요구사항 실기기 확인
- [ ] 개발용 로컬 HTTPS 또는 터널 구성
- [ ] 휴대폰 거치 상태에서 장시간 촬영 테스트
- [ ] 컴퓨터 화면 밝기, 반사광, 모아레 테스트

## 7. 자동 테스트

- [x] 설정 저장/불러오기 테스트
- [x] 중복 주차면 ID 및 잘못된 좌표 검증 테스트
- [x] polygon 교차 비율 테스트
- [x] `occupied`, `empty`, `unknown` 판정 테스트
- [x] 모델 오류 시 `unknown` 처리 테스트
- [x] 캡처 이미지 최근 10장 보관 테스트
- [x] 실제 YOLO 모델 로딩 및 이미지 업로드 API 스모크 테스트
- [x] JavaScript 구문 검사
- [x] 1440px 데스크톱 UI 렌더링 확인
- [x] 390px 모바일 UI 렌더링 및 가로 넘침 검사

## 8. 영상 및 현장 정확도 테스트

- [ ] 테스트 영상 1개 기준 주차면 설정 파일 생성
- [ ] 3~4개 영상별 설정 파일 생성
- [ ] 사람이 판단한 정답과 프로그램 결과 비교
- [ ] 점유 오탐 케이스 기록
- [ ] 비점유 오탐 케이스 기록
- [ ] `unknown` 발생 케이스 기록
- [ ] 실제 5분 주기 분석 로그 확인
- [ ] 실제 촬영 이미지가 10장을 초과하지 않는지 장시간 확인
- [ ] 영상별 분석 속도 측정
- [ ] Homography/BEV 적용 전후 정확도 비교

## 9. 데모 준비

- [ ] 컴퓨터에서 CCTV 연습 영상 재생
- [ ] 휴대폰으로 화면 촬영
- [ ] 휴대폰 웹앱 접속 및 카메라 연결
- [ ] 주차면 설정 시연
- [ ] 모니터링 시작 및 5분 자동 분석 시연
- [ ] 점유율 변화 표시 시연
- [ ] 분석 결과 CSV/JSONL 로그 확인
- [ ] 최근 10장 캡처 보관 정책 시연

## 10. 이후 확장

- [ ] 실제 CCTV 스트림 연동 검토
- [ ] 자동 주차면 후보 추천 검토
- [ ] VLM 보조 판단 검토
- [ ] 관리자 대시보드 검토
- [ ] 현장별 config 관리 기능 검토
- [ ] 소형 PC 또는 엣지 디바이스 배포 검토
